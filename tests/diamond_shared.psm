// Reached from both diamond_left and diamond_right, i.e. via two distinct import
// paths. Import resolution must parse and merge this module exactly once.
extern fn str_equals(a: String borrow, b: String borrow) -> Int

fn shared_double(x: Int) -> Int {
    return x * 2
}

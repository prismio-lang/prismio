// expect-error: soa requires a flat struct with no owned fields; OwnedRow contains non-flat storage
struct OwnedRow {
    name: String
}

fn main() -> Int {
    let mut rows: Vec<OwnedRow> = []
    rows.push(OwnedRow { name: "owned" })
    let view = soa(rows)
    return 0
}

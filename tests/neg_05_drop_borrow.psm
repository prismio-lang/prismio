// expect-error: cannot move out of borrowed value
// A borrowed parameter is non-owning; dropping it would free the caller's value.
struct Res {
    v: Int
}

fn leak(r: Res) {
    drop(r)
}

fn main() -> Int {
    let x = Res { v: 1 }
    leak(x)
    return 0
}

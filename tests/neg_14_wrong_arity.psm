// expect-error: no overload of `println` accepts these argument types
// expect-error: aborting due to 1 previous error
//
// The std.io overload set has no two-argument candidate.

import std.io
fn main() -> Int {
    println(1, 2)
    return 0
}

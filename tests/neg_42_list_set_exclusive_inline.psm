// expect-error: `replace` requires a boxed struct element type

struct Flat {
    x: Int,
    y: Int
}

fn main() -> Int {
    let mut items: Vec<Flat> = []
    items.push(Flat { x: 1, y: 2 })
    items.replace(0, Flat { x: 3, y: 4 })
    return 0
}

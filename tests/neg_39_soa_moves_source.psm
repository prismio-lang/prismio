// expect-error: use of moved value
struct Row {
    value: Int
}

fn main() -> Int {
    let mut rows: Vec<Row> = []
    rows.push(Row { value: 1 })
    let view = soa(rows)
    return rows.length
}

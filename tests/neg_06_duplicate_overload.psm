// expect-error: duplicate definition of `same` with the same parameter types
fn same(value: Int) -> Int {
    return value
}

fn same(other: Int) -> Int {
    return other + 1
}

fn main() -> Int {
    return same(1)
}

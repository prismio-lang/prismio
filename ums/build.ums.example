project("hello") {
    prismio("prismio")

    packages {
        // reserved for future package manager integration
    }

    module("app") {
        source("src/main.psm")
        output("build/hello.exe")

        cSources {
            // reserved for future external C source linking
        }
    }

    run {
        args("")
    }
}
